[defaults]
inventory = inventory.yml
host_key_checking = False
retry_files_enabled = False
# The control node has a broken /etc/ssh/ssh_config.d entry; bypass system ssh_config.
ansible_ssh_common_args = -F /dev/null
